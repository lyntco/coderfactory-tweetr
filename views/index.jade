extends layout

block content
  h1= Tweetr
  div(ng-controller="TweetController")
    form(ng-submit='addTweet(tweet.text)')
      input(type='text', ng-model='tweet.text')
      button TWEEET
    div
      Tweets: {{tweets}}
